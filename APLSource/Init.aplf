 (R msg)←Init
⍝ R=0: no error initialising Selenium
⍝ otherwise error code & message
⍝ (not yet used, but might be one day...)
 R←0
 msg←''

 :If 2=⎕NC'__initialised'
 :AndIf __initialised
     msg←'Selenium already initialised!'
     →0
 :EndIf
 ⎕WX←3

 RETRYLIMIT←DEFAULTRETRYLIMIT←5 ⍝ seconds
 HOME←''    ⍝ the home folder of this ns in the filesystem

 EXT←'.dyalog'
 RIDEEXT←'.ridetest'

 __initialised←1
