 {ok}←id SetInputValue text;s;i;r;obj;js
⍝ Set the value of an input control to text.
 (id obj)←id{2::(⍺)(Find ⍺) ⋄ (⍺.GetAttribute⊂⍵)⍺}'id'  ⍝ if "id" is an element, get its "id" Attibute
 
 obj.Click
 
 (obj text)ExecuteScript'arguments[0].value = arguments[1];'
